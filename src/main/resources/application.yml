spring:
  profiles:
    active: dev
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql:///bootdemo
      username: root
      password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true